import { currentVersion } from './index';

export const PREFIX_RETX_PATH = `${process.env.API_PREFIX_RETX}/${currentVersion}`;

export const PREFIX_IDENTITY_PATH = `${process.env.API_PREFIX_IDENTITY}/${currentVersion}`;

export const PREFIX_TOPUP_PATH = `${process.env.API_PREFIX_TOPUP}/${currentVersion}`;

export const PREFIX_SCM_PATH = `${process.env.API_PREFIX_SCM}/${currentVersion}`;

export const PREFIX_NOTIFICATION_PATH = `${process.env.API_PREFIX_NOTIFICATION}/${currentVersion}`;
